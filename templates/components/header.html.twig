<nav class="navbar navbar-expand-lg navbar-dark bg-dark w-100" {{ attributes }}>

    <div class="container-fluid">
        <div class="navbar-brand" >Towelie Test</div>

        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            {% if user is null %}
                <a class="btn btn-outline-success" href="{{ path('github_connect') }}">Connection</a>
            {% else %}
                <span class="navbar-text me-2">
                        {{ user.name }}
                    </span>
                <a class="btn btn-outline-danger" >Logout</a>
            {% endif %}
        </div>
    </div>
</nav>

